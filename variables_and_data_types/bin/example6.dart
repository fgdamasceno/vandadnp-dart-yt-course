void main(List<String> args) {
  // both variable declarations makes no difference when it comes to typing
  const int age = 20; // just creates more code
  const myAge = 20;
  print(age);
  print(myAge);
}
